import React from "react";
import ReactDOM from "react-dom/client";
import CourseRegisteration from "./Page/CourseRegisteration";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CourseRegisteration />
  </React.StrictMode>
);
